FROM golang:1.24-alpine AS builder
WORKDIR /src
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 go build -o /app/command-service ./cmd/command-service

FROM alpine:3.20
COPY --from=builder /app/command-service /app/command-service
ENTRYPOINT ["/app/command-service"]
